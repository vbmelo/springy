services:
  mongodb:
    image: 'mongo:latest'
    environment:
      - 'MONGO_INITDB_DATABASE=mydatabase'
      - 'MONGO_INITDB_ROOT_USERNAME=vbmelox'
      - 'MONGO_INITDB_ROOT_PASSWORD=admin'
    ports:
      - '27017'
